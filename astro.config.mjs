// astro.config.mjs (VERSIÓ SIMPLIFICADA)
import { defineConfig } from 'astro/config';

// Ja no necessitem la integració de Sanity aquí.
export default defineConfig({
  // Buidem la configuració, Astro ja sap què fer.
});